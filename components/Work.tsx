import React from "react";
import styled from "styled-components";

const StyleWorkSection = styled.section``;

const Work: React.FC = () => {
  return (
    <StyleWorkSection className="container">
      <div className="container-wrapper">Work</div>
    </StyleWorkSection>
  );
};

export default Work;
